# build output (regenerated by the Action every run)
public/

# python junk
__pycache__/
*.pyc

# reproducible indicator-validation input cache and intermediate output
validation/

# local logs
run_err.log
run_forever.log

# NOTE: the *_state.json files ARE committed on purpose — they seed the first
# Cloudflare build so it still shows data even if Yahoo rate-limits the runner.

# not needed for hosting
archive/
Stock Sim/
